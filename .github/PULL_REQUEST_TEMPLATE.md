## What changes

<!-- What this does, and why. If it fixes a defect, say what the defect actually was — the
     behaviour someone observed, not the symptom's name. -->

## How it was verified

<!-- What you ran or read, and what it returned. "Tests pass" is weaker than the output.
     If you verified against a live cluster, name the cluster and the field you read. -->

## Scope

- [ ] No CRD, schema or API change — or, if there is one, it is described above
- [ ] Chart version moved if chart contents changed
- [ ] `-crd` / lockstep pins moved together, if applicable

## Risk

<!-- What breaks if this is wrong, and how it would show. Say explicitly if a failure would be
     SILENT — those are the ones worth calling out, because nothing else will. -->
